"use client";

import { DiscoveryCommunityView } from "@/components/app-shell/views/discovery-community-view";

/**
 * Discovery Page - Client Component
 *
 * Renders the community discovery view.
 * Auth validation is handled by the (main) layout.
 */
export default function DiscoveryPage() {
  return <DiscoveryCommunityView />;
}
